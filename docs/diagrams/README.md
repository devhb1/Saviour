# Diagram sources

Plain Mermaid (`.mmd`) for [Excalidraw → Mermaid to Excalidraw](https://excalidraw.com).

| Import | How |
|---|---|
| One diagram | Open `.mmd` → copy all → Excalidraw Mermaid import |
| All + captions | Use [`../DIAGRAMS.md`](../DIAGRAMS.md) |
| Narrative | [`../ARCHITECTURE.md`](../ARCHITECTURE.md) |

| File | Topic |
|---|---|
| `01`–`16` | Product loop → surfaces (see filenames) |
| `17-clone-cascade.mmd` | Address miss → bytecode → `code-<hash>.saviours.eth` |

Flowcharts import as editable Excalidraw elements. Keep labels ASCII-friendly if a paste fails on special punctuation.
