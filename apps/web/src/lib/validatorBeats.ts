/** Rotating copy while the deterministic validator is the long wait. */
export const VALIDATOR_BEATS = [
  "Validator deciding…",
  "Rules only — AI cannot invent TAINTED…",
  "Checking FLASHLOAN_ONE_SHOT ∧ ATOMIC_MULTI_PROTOCOL…",
  "Comparing cited signals to threat paths…",
  "Still deciding — Graph evidence is the only input…",
  "Validator deciding… almost there…",
] as const;
